<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tsw.task.dao.TaskDao">
	<insert id="doWrite">
		INSERT TASK
		SET regDate = NOW(),
		updateDate = NOW(),
		title = #{title},
		`body` = #{body},
		taskPartId = 2,
		memberId = 2
	</insert>

	<select id="getTask" resultType="Task">
		SELECT *
		FROM task
		WHERE id =
		#{id}
	</select>

	<select id="getNumTask" resultType="int">
		SELECT COUNT(id)
		FROM task
	</select>

	<update id="doDelete">
		UPDATE TASK
		SET delStatus=TRUE,
		delDate = NOW()
		WHERE id
		= #{id};
	</update>

	<select id="isDelete" resultType="boolean">
		SELECT delStatus
		FROM TASK
		WHERE
		id = #{id};
	</select>

	<update id="doModify">
		UPDATE TASK
		SET title = #{title},
		body = #{body},
		updateDate = NOW()
		WHERE id = #{id};
	</update>



</mapper>



